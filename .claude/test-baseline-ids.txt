# Test baseline — accepted pre-existing failures (gate = per-unit failing set must be a SUBSET of this).
# Re-derived 2026-07-19 (Phase 0, resume-porting) at HEAD 5a9d30763, forcedotcom target b5d37d807.
# SDKCore build ✓ 497 pass / 3 fail. SmartStore build ✓ 176 pass / 1 fail. MobileSync: build ✓;
# full run blocked by documented live-auth hang in live-gated class func setUp() (oracle hangs identically,
# see live-org-skip-ledger.md) — no non-skip MobileSync failure observed; runnable subset not summarizable.
# NOTE: prior entries testRedirect + testOpenIDToken are LIVE-GATED → now SKIP (removed, no longer fail).
# testGetGlobalStoreNames did not fail this run (removed). Shrinking ratchet — do not baseline your way to green.
SFSmartSqlTests/testCleanupRegexpFaster
BiometricAuthenticationManagerTests/testNotEnabled
RestClientWebSocketTests/testNewWebSocketFromURLRequest
SFUserAccountManagerTests/testLogin
